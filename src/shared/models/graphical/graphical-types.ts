//try out: new helper for message positioning
export interface Position {
  x: number;
  y: number;
  width: number;
  heigt: number;
}

export enum ArrowHead {
  POINTER,
  ATTACK,
  SUPPORT,
  SUPPLEMENT,
}
