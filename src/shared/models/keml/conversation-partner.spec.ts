import { ConversationPartner } from './conversation-partner';

describe('ConversationPartner', () => {
  it('should create an instance', () => {
    expect(new ConversationPartner()).toBeTruthy();
  });
});
