import { ConstructorPointers } from './constructor-pointers';

describe('ConstructorPointers', () => {
  it('should create an instance', () => {
    expect(new ConstructorPointers()).toBeTruthy();
  });
});
