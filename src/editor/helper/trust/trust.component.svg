<svg:g >
  <rect [attr.x]="x" [attr.y]="y" [attr.width]="w" [attr.height]="h" [attr.fill]="color"></rect>
  <text [attr.x]="0" [attr.y]="0" font-size="100%" [attr.transform]="'translate('+x +','+ y +') rotate(90)'">{{trust}}</text>
</svg:g>
