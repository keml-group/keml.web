<svg:g >
  <rect [attr.x]="x" [attr.y]="y" [attr.width]="w" [attr.height]="h" [attr.fill]="color"></rect>
  <text [attr.x]="x" [attr.y]="y" font-size="100%" transform="rotate(90)">{{trust}}</text>
</svg:g>
