import { LlmConversationCreator } from './llm-conversation-creator';

describe('LlmConversationCreator', () => {
  it('should create an instance', () => {
    expect(new LlmConversationCreator()).toBeTruthy();
  });
});
